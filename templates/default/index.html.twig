{% extends 'base.html.twig' %}

{% block title %}Accueil - Tutoriels{% endblock %}

{% block body %}
    <div style="text-align: center; margin-top: 50px;">
        <h1>Bienvenue sur notre plateforme de Tutoriels ! 🎓</h1>
        <p>Découvrez une collection de tutoriels pour apprendre et progresser.</p>
        {% if app.user %}
            <p>Bonjour, {{ app.user.firstName }} {{ app.user.lastName }} !</p>
            <a href="{{ path('app_profile') }}" style="margin: 10px; padding: 10px; background-color: #4CAF50; color: white; text-decoration: none;">Accéder à mon profil</a>
        {% else %}
            <a href="{{ path('app_login') }}" style="margin: 10px; padding: 10px; background-color: #008CBA; color: white; text-decoration: none;">Se connecter</a>
            <a href="{{ path('app_register') }}" style="margin: 10px; padding: 10px; background-color: #f0ad4e; color: white; text-decoration: none;">Créer un compte</a>
        {% endif %}
    </div>
{% endblock %}
